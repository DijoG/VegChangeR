% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VegChangeR.R
\name{get_VC_optimized}
\alias{get_VC_optimized}
\title{Calculate vegetation changes (gain/loss only)}
\usage{
get_VC_optimized(inputRAST, targetDATE, processing_option = "auto")
}
\arguments{
\item{inputRAST}{A SpatRaster object containing vegetation data with dates in layer names}

\item{targetDATE}{Target date for analysis obtained from names of input raster stack}

\item{processing_option}{Memory processing option: "auto", "lowmem", "medium",
"high", or "aggressive". Default: "auto"}
}
\value{
A list containing SpatRaster objects with vegetation changes where:
\itemize{
\item{-1: Vegetation loss}
\item{1: Vegetation gain}
\item{NA: Stable vegetation or non-vegetated areas}
}
}
\description{
Simplified version that only returns vegetation gain (+1) and loss (-1),
masking out stable vegetation and no-change areas.
}
\examples{
\dontrun{
# Load your vegetation raster stack
veg_data <- rast("path/to/vegetation_data.tif")

# Obtain stack layer names
names(veg_data)

# Calculate changes for specific date
changes <- get_VC_optimized(veg_data, "2023-06-15", processing_option = "medium")

# Plot results
plot(changes$oneY)
}

}
